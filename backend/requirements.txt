# ==============================================================================
# QuantumLeap AI Trader - Production Dependencies
# ==============================================================================

# --- FastAPI & Web Server ---
# Gunicorn is the production WSGI server, Uvicorn is the worker class.
fastapi==0.111.0
gunicorn==22.0.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9

# --- Asynchronous Networking ---
aiohttp==3.9.5
httpx==0.27.0

# --- Database & ORM ---
# SQLAlchemy is the ORM, asyncpg for PostgreSQL (production), aiosqlite for local dev.
SQLAlchemy[asyncio]==2.0.29
asyncpg==0.29.0
aiosqlite==0.19.0

# --- Data Validation & Settings ---
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1

# --- Security, Auth & JWT ---
# firebase-admin for auth, python-jose for JWT, passlib/bcrypt for hashing.
# NOTE: bcrypt is pinned to a specific version to maintain compatibility with passlib.
firebase-admin==6.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
cryptography==42.0.5

# --- Trading & Exchange Integration ---
ccxt==4.3.46
tradingview-ta==3.3.0

# --- AI & Machine Learning ---
# Pinned to ensure compatibility between libraries.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
tf-keras==2.16.0
# Note: Installing tf-keras will pull in a compatible version of tensorflow.
nltk==3.8.1
vaderSentiment==3.3.2
google-generativeai==0.5.4
xgboost==2.0.3
pandas-ta==0.3.14b0

# --- Telegram Bot ---
python-telegram-bot[job-queue]==21.1.1

# --- Utilities ---
slowapi==0.1.9
joblib==1.4.2
